# Email Configuration
# Set your email address to receive contact form submissions
RECIPIENT_EMAIL=your-email@example.com

# Optional: SMTP Settings (for sending emails through your own email server)
# If not provided, contact messages will be logged to console
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Database (already configured)
# DATABASE_URL=your-database-url